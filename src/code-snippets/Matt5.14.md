Matthew 5:14

```csharp
var Light = new Light<World>();
var Christian = Light; // "You are the light of the world."

if (City.Location == Location.SetOnAHill)
{
    _ = !CanBeHidden(City); // "A city set on a hill cannot be hidden."
    Shine(Light);
}
```